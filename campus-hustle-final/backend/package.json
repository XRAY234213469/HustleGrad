{
  "name": "hustlegrad-backend",
  "version": "2.0.0",
  "description": "HustleGrad — Campus Peer-to-Peer Marketplace (refactored)",
  "main": "server.js",
  "scripts": {
    "prestart": "npx kill-port 5000",
    "start": "node server.js",
    "predev": "npx kill-port 5000",
    "dev": "nodemon server.js",
    "db:init": "psql $DATABASE_URL -f schema.sql"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.13",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
